import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'via.placeholder.com',
      'picsum.photos',
      'source.unsplash.com',
      'cdn.pixabay.com',
      'images.pexels.com'
    ],
  },
};

export default nextConfig;
